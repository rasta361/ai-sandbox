# Git identity for Claude's commits
GIT_USER_NAME="Claude (AI Assistant)"
GIT_USER_EMAIL="claude@yourdomain.com"

# Port range to expose from container to host (for dev servers, etc.)
# Default: ports 18000-18010
SANDBOX_PORT_START=18000
SANDBOX_PORT_END=18010
